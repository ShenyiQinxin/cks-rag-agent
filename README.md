## 1. Create project + virtual environment

```sh
python3 --version    
pip --version 

mkdir cks-rag-agent && cd cks-rag-agent
python3 -m venv .venv
source .venv/bin/activate

python --version    

      
```

## 2. Create requirements.txt


